<%= form_for @betty, html: { multipart: true } do |f| %>
  <% if @betty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@betty.errors.count, "error") %> prohibited this betty from being saved:</h2>

      <ul>
      <% @betty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control" %>
  </div>

 <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
